Protocol renamed BSP → BEST (BEhavioral STate; full name Behavioral State Protocol unchanged) and the message envelope is now a conformant CloudEvents 1.0 profile: absolute dataschema (the catalogue URI), source as URI-reference, unknown envelope attributes ignored rather than rejected. Adds MIGRATION.md (0.8.x → 0.9.0 checklist) and standards-track artifacts (IANA well-known URI registration template, Internet-Draft skeleton). Companion MCP server republished as @behavioralstate/best-mcp 2.0.0 with legacy BSP_* env-var fallback.
