Adds the consolidated single-file SPEC.md, fixes example dataschema URIs, and syncs the OpenAPI document with the spec surface (queries, SSE stream, event schema endpoints). Released under the protocol's previous short name, BSP.
