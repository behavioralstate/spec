Website and editorial release — **no normative changes**. Every 0.9.2 message, manifest, and endpoint remains exactly as specified; there is nothing to migrate.

## Website

- New **[/roadmap](https://behavioralstate.io/roadmap)** page rendering the repository's `ROADMAP.md`: where the protocol stands, the standards track (IANA registration and Internet-Draft, both submitted 2026-08-24), open TODOs, and the evolution candidates identified from the IETF dawn/agentproto BoF landscape. Linked from the site navigation and included in the sitemap.

## Editorial

- `ROADMAP.md` updated: standards submissions recorded (IANA provisional registration emailed; [draft-dinuzzo-best-protocol-00](https://datatracker.ietf.org/doc/draft-dinuzzo-best-protocol/) posted, ISE outcome noted), evolution candidates filed from the dawn/agentproto draft review, and a correction — command idempotency/replay protection is already normative in SPEC.md (envelope `id` as idempotency key), so the remaining design item is retention-window declaration only.

## Tooling (released separately, referenced here for the record)

- `@behavioralstate/best-validate` 0.1.1 — `--legacy-bsp` mode removed; both known deployments are on 0.9.x.
- `@behavioralstate/best-mcp` 2.2.1 — surfaces 0.9.2 correlation: optional `correlation_id` on the send tools, echoed `correlationId` returned with guidance to filter the event tools by it. Verified byte-identical behaviour against pre-0.9.2 endpoints.
