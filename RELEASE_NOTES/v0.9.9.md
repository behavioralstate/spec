Editorial release — no protocol or schema changes; 0.9.8 manifests remain conformant byte-for-byte. No migration steps required.

## What changed

- **Website hero copy** shortened to be punchier: "Give an agent a machine-readable description of what your service can do — and let it figure out the workflow. Discover commands, queries, and recipes through a manifest, with no SDK or scraping required."
- **Quickstart Prompt 1** (`specs/quickstart.md`, *Make your service BEST-compliant*) simplified — dropped the verbose endpoint checklist and auth details in favor of two lines: the compliance request and a spec reference (`https://behavioralstate.io/specs`). The "What to expect" text was updated to match.

## Tooling

- `@behavioralstate/best-validate` — no release needed: nothing wire- or schema-facing changed.
